Public-Transit
==============
Java based algorithm to solve the D.C. transit system.
The program takes the input from terminal.
  The input files are included in the Input folder.
The program uses modified version of Dijkstra's algoritm to solve the problem.
